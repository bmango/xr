1. $ git clone https://github.com/bmango/xr.git .
2. Adjust subdomain path to "web" directory
3. $ echo '$settings["config_sync_directory"] = "../config/sync";' >> web/sites/default/default.settings.php
